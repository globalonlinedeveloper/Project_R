# Stage-3 LOCAL build backlog (no money · no live Supabase · all reversible via git)

> Produced by the agent-driven Stage-4 security review (Session 18) and verified against the real repo. **Part 1 is fully buildable + testable locally for FREE** — the keystone (`pgserver`, a root-free PostgreSQL with a working `pg_dump`) was booted end-to-end in the sandbox (created the FSRS tables, dumped the schema, exit 0). Part 2 needs the owner to create accounts / spend. Nothing here touches the live Supabase project; every step is `git revert`-able.
>
> **Chain-state header (for autonomous / multi-session execution):** RUN_STATUS: running (S19 @ 2026-06-23T11:53:16Z) · NEXT: L0 · GATE = python `pytest ratel-tools/tests` green locally + (Dart items) flutter-gate green in CI · PUSH after every item.

## What was proven this session
- `pip install --user --break-system-packages pgserver pg8000` works; bundled `pg_dump` at `…/site-packages/pgserver/pginstall/bin/pg_dump`; boot → DDL → `pg_dump --schema-only` exit 0. This is the disposable DB the checklist Part D requires.
- The repo has **no DDL generator** today (only `ratel-tools/codegen_dart.py`). `schema/schema.json` currently holds only the 9 content tables — the user tables (finding P0-2) are genuinely absent.
- `flutter`/`dart` are CI-only in this sandbox; **Python (`pytest`) runs fully locally** — so all DB/RLS/schema work is locally verifiable; Dart-side changes are verified in CI.

## PART 1 — LOCAL / FREE (execute in order; push each gate-green)

- **L0 — unfreeze gate + tooling pin.** Add a dated owner-authorized "schema un-frozen for the Stage-3 user-table extension (reversible via git)" note to `docs/SCHEMA_LOCK.md`; add `pgserver`+`pg8000` to a new `ratel-tools/requirements-dev.txt`. *(Only L0/L1 touch the frozen schema.)*
- **L1 — [P0-2] author the user tables into `schema.json` + Dart codegen.** Entities from R-G6/R-J3/R-J7: `user` (FSRS-7 weights, `pro_until`), `user_course` (`theta_per_skill`), `user_item_state` (FSRS: stability/difficulty/due/reps/lapses/state, `(user_id,due)` intent), `user_phoneme_state`, `placement_session`, `review_log` (append-only, born `pg_partman` monthly-partitioned, kept forever, R-M), `credit_ledger` (double-entry grant/spend/refund + `client_event_id` UUID UNIQUE idempotency). New `schema/tables/*.schema.json` mirroring the existing style (`additionalProperties:false`); add `user_id` uuid def (R-K6) + enums (`fsrs_state`, `ledger_entry_type`, `grant_source`). Wire `schema_loader.py` + `codegen_dart.py` TABLES. **Verify:** `pytest ratel-tools/tests` (schema-lint + a valid-row + a rows-only-violation per new table); Dart codegen + tests in CI. **Do NOT wire features to them (Stage-3).**
- **L2 — [Part D] build the DDL generator** `ratel-tools/codegen_ddl.py` → `schema/sql/0001_schema.sql` (JSON-Schema→PG types; enums→`CREATE TYPE`; open object→`jsonb`; `review_log` `PARTITION BY RANGE`; `user_item_state (user_id,due)` index; fail-loud on any unrepresentable construct; deterministic ordering).
- **L3 — [Part D] `pg_dump diff = 0`** `ratel-tools/tests/test_pg_dump_parity.py`: boot `pgserver`, apply `0001_schema.sql`, `pg_dump --schema-only`, normalize both + assert empty diff. Record command + artifacts into `STAGE4_SIGNOFF_CHECKLIST.md` Part D. (`importorskip` so CI without pgserver stays green.)
- **L4 — [P0-3] entitlement/credit RLS client-READ-ONLY + negative tests.** `schema/sql/0002_rls_entitlement.sql` (SELECT-only for `authenticated` on `pro_until`/`credit_ledger`; writes = `service_role` only). `test_rls_entitlement.py` on pgserver: simulate `auth.uid()` via a GUC + roles; **negative asserts** that a user CANNOT UPDATE own `pro_until` or INSERT a self credit-grant (the "free Pro / infinite credits" hole), positive that service_role can.
- **L5 — [TS-2/TS-3] generic deny-by-default RLS for every user table + isolation tests.** `0003_rls_all_tables.sql` (ENABLE+FORCE RLS, `USING/WITH CHECK (auth.uid()=user_id)`); `test_rls_isolation.py` parameterized over the table list (asserts RLS enabled, cross-user SELECT/UPDATE = 0 rows, `review_log` foreign-uid insert denied) — a new user table without a policy fails by construction. Add a CI `pytest` step.
- **L6 — [P2-1] enum forward-compat decision + guard.** Decide hard-reject (recommended default, fail-closed) vs graceful-degrade (`unknownEnumValue`) per enum; encode in `codegen_dart.py` `gen_enum()`; record the decision in `STAGE4_ONEWAY_DOORS.md` (door #18); add a roundtrip test for the chosen behavior.
- **L7 — [TS-13/TS-11] cheap client guards.** TS-13: AI/relay output treated as untrusted (a `RelayText` wrapper + "escape before any HTML/markdown sink" contract + test). TS-11: `Identity.claimAnonymousState`-shaped contract that takes a SERVER-issued token, never a client anon id + a guard test. Interface+test only, no backend.
- **close-out:** extend `ci.yml` with `pytest …/test_pg_dump_parity.py …/test_rls_*.py` (skips cleanly if pgserver absent); fill `STAGE4_SIGNOFF_CHECKLIST.md` Part D evidence.

## PART 2 — LIVE / PAID (needs the owner to create accounts / spend; logic+local tests buildable first)
- **P0-6** service-role key handling/rotation — build the read-from-`Deno.env` Edge code + a CI secret-scan test; rotation needs a real project.
- **P0-7a** credit-minting — double-entry ledger + idempotency fully unit-testable on pgserver; device attestation (Play Integrity/App Attest) + Turnstile need real accounts/devices.
- **P0-7b** AI-relay moderation — build the fail-CLOSED moderation state machine + tests vs a stub; real moderation needs an OpenAI/Gemini key.
- **P1-1** payments — webhook signature-verify + idempotency + clawback unit-testable with synthetic payloads; real receipts/keys are live-only.
- **P1-6** Edge perimeter — R2 signed-URL TTL/scope + Pro-only-audio authz logic + tests; real R2/Turnstile/KV need Cloudflare.
- **AI relay (R-H7)** — full Gemini adapter + R-M8 cost guardrail behind the existing fail-closed seam, fake-transport tests; deploy + key are owner-gated.

## Honest gate status after Part 1
The two today-unsatisfiable checklist preconditions become **legitimately verified, locally + free**: the user tables exist in the single source of truth (P0-2) and **Part D `pg_dump diff = 0` actually runs** on a disposable DB (live project untouched); the RLS the threat model only *describes* becomes **executed + negatively-tested** SQL (incl. the no-self-grant proof, P0-3). **Still needs a human / live deploy:** the human dual architect sign-off (Part E, by R-O1 — agent review can inform it but not replace the signatures), and every Part-2 item until the owner provisions Supabase/Stripe/Play/Cloudflare/Gemini. Net: a real, reversible, zero-spend step from "prerequisites not met" → "all locally-provable prerequisites met + evidenced; gate open pending human sign-off + money-gated live validation."
