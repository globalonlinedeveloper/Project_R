/// Config & experimentation seams — the feature-flag / A-B / rollout evaluation
/// core (FLAGS-1). Pure decision logic; the live config transport is injected.
library;

export 'feature_flags.dart';
