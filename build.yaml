targets:
  $default:
    builders:
      json_serializable:
        options:
          # nested freezed objects must call their own toJson() (else they leak as objects)
          explicit_to_json: true
          create_to_json: true
          create_factory: true
